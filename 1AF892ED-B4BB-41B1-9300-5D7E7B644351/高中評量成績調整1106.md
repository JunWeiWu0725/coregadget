# 修正 `current.template` 與 `templateList` 物件參考不一致問題 - 完成報告

> **完成日期：** 2025-11-06  
> **修改檔案：** `js/gradebook.js`, `content.htm`

## 問題描述

當 `templateList` 因重載課程/樣板而被重建時，`current.template` 仍指向舊物件，導致 `ng-repeat` 的物件參考比對失效，造成 UI 顯示「不在選單內」的問題。

## 解決方案

採用 **ExamID** 作為唯一選取依據，重建後以 ExamID 回填對應的新物件，並於 HTML 使用 `track by` 穩定渲染。

## 實作內容

### ✅ 1. 初始化 `current.templateId` 作為單一事實來源

**檔案：** `js/gradebook.js` (第 92 行)

```javascript
$scope.current = {
    SelectMode: "Seq.",
    SelectSeatNo: "",
    Value: "",
    Student: null,
    Exam: null,
    Course: null,
    SchoolYear: "",
    Semester: "",
    templateId: null, // [TemplateGuard] 以 ExamID 作為唯一選取依據
};
```

### ✅ 2. 建立 `rebuildTemplateList` 函式

**檔案：** `js/gradebook.js` (第 298-320 行)

```javascript
/**
 * [TemplateGuard] 重建 templateList 後回填 current.template
 * 以 templateId 對應回新陣列中的物件，避免參考失配
 */
function rebuildTemplateList(newList) {
    $scope.templateList = [].concat(newList || []);

    // 以 ID 對應回新陣列中的物件
    if ($scope.current.templateId) {
        var hit = $scope.templateList.find(function (t) { return t.ExamID === $scope.current.templateId; });
        if (hit) {
            $scope.current.template = hit;
        } else {
            // 找不到同 ID，退回第一筆或 null
            $scope.current.template = $scope.templateList[0] || null;
            $scope.current.templateId = $scope.current.template ? $scope.current.template.ExamID : null;
        }
    } else {
        // 尚未選取過，預設第一筆
        $scope.current.template = $scope.templateList[0] || null;
        $scope.current.templateId = $scope.current.template ? $scope.current.template.ExamID : null;
    }
}
```

### ✅ 3. 修改 `setCurrentTemplate` 函式

**檔案：** `js/gradebook.js` (第 1213-1247 行)

- 加入 `templateId` 的更新邏輯
- 以 `template.ExamID` 作為唯一依據儲存

```javascript
$scope.setCurrentTemplate = function (template) {
    if (!template) return; // [TemplateGuard] 防護檢查
    
    // ... 資料檢查邏輯 ...
    
    if (execute) {
        // [TemplateGuard] 以 ID 作為唯一依據
        $scope.current.templateId = template.ExamID;
        $scope.current.template = template; // 保留供舊邏輯使用，但不再當唯一依據
        // ... 後續處理 ...
    }
}
```

### ✅ 4. 更新 `setCurrentCourse` 使用 `rebuildTemplateList`

**檔案：** `js/gradebook.js` (第 458-645 行)

- 將直接賦值 `$scope.templateList = []` 改為先建構暫存清單
- 在建構完成後呼叫 `rebuildTemplateList()` 回填

```javascript
// 課程評分樣板：定期評量清單
var builtTemplateList = []; // [TemplateGuard] 先建構暫存清單
// ... 建構邏輯 ...
builtTemplateList.push(temp); // [TemplateGuard] 先加入暫存清單
// ...
// [TemplateGuard] 以封裝函式回填 current.template / templateId
rebuildTemplateList(builtTemplateList);
```

### ✅ 5. 更新 `dataReload` 中的模板檢查邏輯

**檔案：** `js/gradebook.js` (第 1191-1208 行)

- 將以 `Name` 比對改為以 `ExamID` (`templateId`) 比對

```javascript
if ($scope.current.mode == '平時評量') {
    var chk = false;

    // [TemplateGuard] 改用 ExamID 比對而非 Name
    if ($scope.current.templateId) {
        chk = $scope.templateList.some(function (rec) {
            return rec.ExamID === $scope.current.templateId;
        });
    }
    // ... 後續處理 ...
}
```

### ✅ 6. 更新 `reloadGradeItem` 確保同步

**檔案：** `js/gradebook.js` (第 2166-2172 行)

- 確保 `templateId` 與 `template` 同步

```javascript
// [TemplateGuard] 確保 templateId 與 template 同步
if (!$scope.current.template && $scope.templateList[0]) {
    $scope.current.template = $scope.templateList[0];
    $scope.current.templateId = $scope.current.template.ExamID;
} else if ($scope.current.template && !$scope.current.templateId) {
    $scope.current.templateId = $scope.current.template.ExamID;
}
```

### ✅ 7. 更新 HTML 模板使用 `track by` 和 ExamID 比對

**檔案：** `content.htm` (第 197-205 行)

```html
<div class="btn-group" ng-if="current.mode == '平時評量'">
    <a class="btn btn-default dropdown-toggle" data-toggle="dropdown"
        ng-if="templateList.length > 0">{{current.template.Name}}&nbsp;<span class="caret"></span></a>
    <ul class="dropdown-menu">
        <li ng-repeat="tpl in templateList track by tpl.ExamID" ng-class="{active: tpl.ExamID === current.templateId}">
            <a href="javascript:;" ng-click="setCurrentTemplate(tpl)">{{tpl.Name}}</a>
        </li>
    </ul>
</div>
```

**變更重點：**
- 加入 `track by tpl.ExamID` 穩定渲染
- `ng-class` 改為 `tpl.ExamID === current.templateId` 而非 `template == current.template`

## 變更摘要

### 修改的檔案

1. **`js/gradebook.js`**
   - 新增 `current.templateId` 初始化
   - 新增 `rebuildTemplateList()` 函式
   - 修改 `setCurrentTemplate()` 函式
   - 修改 `setCurrentCourse()` 函式
   - 修改 `dataReload()` 函式中的模板檢查邏輯
   - 修改 `reloadGradeItem()` 函式確保同步

2. **`content.htm`**
   - 更新試別切換區塊的 `ng-repeat` 和 `ng-class`

### 變更標記

所有相關變更均標記 `// [TemplateGuard]` 註解，方便日後追蹤與維護。

## 測試建議

1. **切換課程測試**
   - 切換課程 A → B → A，確認回到 A 時 `current.template` 與選單 active 狀態一致

2. **重載樣板測試**
   - 重載樣板（造成 `templateList` 新陣列）時，確認 active 項維持不變或正確回到第一筆

3. **功能相容性測試**
   - 成績管理：輸入、儲存功能正常
   - 平時評量：切換試別後可以正確讀取/儲存對應試別的平時項目與試算

## 風險控制

- ✅ 所有直接賦值 `templateList` 的地方已改為使用 `rebuildTemplateList()`
- ✅ 保留 `current.template` 以供舊邏輯使用，確保向後相容
- ✅ 使用 `ExamID` 作為唯一識別，避免名稱重複問題

## 預期效果

1. ✅ 切換/重載課程與樣板時，`current.template` 與選單狀態不再失配
2. ✅ UI 不再顯示「不在選項內」的錯誤狀態
3. ✅ `ng-repeat` 使用 `track by` 穩定渲染，避免不必要的 DOM 重建
4. ✅ 不影響成績管理與平時評量的既有寫入流程

## 備註

- 這是前端參考一致性的修復，不改後端 API 與資料格式
- 採用 ID 為單一事實來源的寫法，是避免 AngularJS 物件參考漂移最穩妥的方式
- 所有變更均標記 `[TemplateGuard]` 註解，方便日後維護與追蹤

